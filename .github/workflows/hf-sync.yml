# Archived — deployment moved to Vercel (auto-deploys from GitHub).
# See docs/DEPLOYMENT.md for current deployment instructions.
#
# This workflow previously synced to HuggingFace Spaces.
# Kept as reference but disabled (no trigger).
